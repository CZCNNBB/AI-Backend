# 示例环境变量 - 复制为 .env 后修改实际值
# 后端服务地址（与后端 .env 中的 FASTAPI_PORT 保持一致，常见 8090）
# 注意：必须用 127.0.0.1，不要用 localhost
# 因为 Node.js 解析 localhost 时默认优先走 IPv6 [::1]，
# 而 uvicorn 默认只监听 IPv4 127.0.0.1，会导致 ECONNREFUSED
VITE_BACKEND_URL=http://127.0.0.1:8090

# 前端 API 前缀
VITE_API_BASE=/api
